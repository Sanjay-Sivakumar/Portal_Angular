{
  "name": "backendservice",
  "version": "1.0.0",
  "description": "node for backend service",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sanjay",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "request": "^2.88.2",
    "unirest": "^0.6.0"
  }
}
